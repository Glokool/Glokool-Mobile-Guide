import React from 'react';
import { Platform, StyleSheet, Text,  } from 'react-native'
import { Layout, Button } from '@ui-kitten/components'

export const ProfileDetailScene = ()=> {
    return (
        <Layout>
            
        </Layout>
    )
}