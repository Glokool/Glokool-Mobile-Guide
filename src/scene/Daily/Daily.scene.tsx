import React from 'react';
import { StyleSheet, Platform, Text } from 'react-native';
import { Layout } from '@ui-kitten/components';

export const DailyMainScene = (props: DailyMainSceneProps) => {
    return (
        <Layout>
            <Text>asfddasfdasfdsaf</Text>
        </Layout>
    )
}