import React, { } from 'react';
import { StyleSheet, Platform, Text, TouchableOpacity, Alert } from 'react-native';
import { Layout } from '@ui-kitten/components';

export const ChatReportScene = () => {
    return (
        <Layout>
            
        </Layout>
    )
}